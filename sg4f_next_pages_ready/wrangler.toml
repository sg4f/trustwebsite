name = "trustwebsite"

# Required so Pages accepts this file for your project:
pages_build_output_dir = "./.vercel/output/static"

# Node.js compatibility for the runtime:
compatibility_date = "2026-03-09"      # any date ≥ 2024-09-23 is fine
compatibility_flags = ["nodejs_compat"]
``
